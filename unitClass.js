function createUnit(classType, pos, currentHp, group)
{
    if(classType='tiger')
    {
        return {pos:12, maxHp: 100, currentHp: 57, group: 1, atak: 30, isMoving: false, oldPos: {}, moveRange: 3, isRange: false, classType: 'tiger'};
    }
}